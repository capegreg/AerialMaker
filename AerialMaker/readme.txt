node modules to install
-----------------------

# For windows service development
# Install node.js runtime

https://nodejs.org/en/

# For database, use oracledb min version 4.2
npm install oracledb@latest

# For image file name
npm install uuid

# For image processing
npm install sharp

npm install @esri/arcgis-rest-request @esri/arcgis-rest-auth cross-fetch isomorphic-form-data

# For windows service
# Dep. 	node.js
#				python
npm install os-service

# add email sending
npm install nodemailer@latest
